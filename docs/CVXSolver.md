# CVXSolver

The CVXSolver class is a child of the Solver class. Calling allows us to
instantiate the solver with the CVX algorithm and our selected options.

## Constructor
```matlab
>> solver = od.CVXSolver(problem, u_dim, options)
```

**Optinal parameters**
